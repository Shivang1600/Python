# Create a dictionary from the two lists below. 
# The first list should be the keys and the 2nd list should be the corresponding values.


x = [1, 2, 3, 4]

y = ['one', 'two', 'three', 'four']

    
print(dict(zip(x,y)))